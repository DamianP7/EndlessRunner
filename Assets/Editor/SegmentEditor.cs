using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;
/*
[CustomEditor(typeof(Segment))]
public class SegmentEditor : Editor
{
	public override void OnInspectorGUI()
	{
		DrawDefaultInspector();
		Segment myScript = (Segment)target;

		foreach (int item in myScript.coinPositions.Keys)
		{
			EditorGUILayout.BeginHorizontal();
			EditorGUILayout.IntField(item);
			myScript.coinPositions item.Value = EditorGUILayout.ObjectField();

			EditorGUILayout.EndHorizontal();
		}

	}
}
*/